# 数据目录说明

这里放本地运行数据和备份。

## 文件

- `campus_lite.db`：本地 SQLite 数据库。
- `novel_state_backups/`：小说状态、数据库重置或迁移前的备份。

## 原则

- 本地数据库属于运行数据，不当作稳定源码结构。
- 结构性修复可以重建数据库，但重建前必须先备份。
- 小说状态或版本语义调整前，优先把当前数据库复制到 `novel_state_backups/`。
