# 前端功能目录说明

前端功能辅助代码放在这里，`App.vue` 负责页面组合和交互编排。

## 目录

- `chat/`：聊天 API。
- `novel/`：小说 API、画布工具、常量。
- `personalityTest/`：人格测试题库、存储、结果图导出。

## 原则

- 新接口放对应 `api.ts`。
- 纯函数、常量和本地存储逻辑不要继续堆进 `App.vue`。
- 页面级状态仍在 `App.vue`，功能目录只放可复用的功能边界。
