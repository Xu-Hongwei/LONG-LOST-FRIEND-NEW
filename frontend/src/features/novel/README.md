# Novel Frontend Feature

这里封装小说工作台的前端工具。

## 文件

- `api.ts`：小说项目、画布、章节、版本、检查、生成、指令优化请求。
- `canvas.ts`：Story Canvas 和 scene card 的前端纯函数。
- `constants.ts`：状态标签、流程步骤、字段配置、默认指令。

## 原则

- 画布字段可以为章节提供默认值，但不能覆盖用户已编辑的章节字段。
- 前端只准备 draft，最终保存以后端原子接口为准。
- 小说章节草稿、画布草稿和生成进度要分开维护。
